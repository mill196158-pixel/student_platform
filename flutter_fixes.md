# 🔧 ИСПРАВЛЕНИЯ В FLUTTER КОДЕ

## 📋 Анализ текущего состояния:

### ✅ Что уже работает:
- **Модель `Message`** поддерживает `attachments: List<ChatFile>?`
- **`MessageBubble`** получает `attachments: m.attachments`
- **`MultiFileBubble`** уже создан и готов к использованию

### ❌ Что нужно исправить:

## 1. ИСПРАВЛЕНИЕ В CHAT_TAB.DART

### Проблема: Хардкод team_id
```dart
// СТРОКА 420: ЗАМЕНИТЬ
final chatId = await _getChatIdForTeam('7f0a7234-9565-4db4-9123-98c852740a6b');

// НА:
final teamId = context.read<TeamCubit>().state.team.id;
final chatId = await _getChatIdForTeam(teamId);
```

### Проблема: Логика отправки мульти-файлов
Нужно добавить логику для:
- Отмены загрузки файлов (крестик)
- Добавления текста к файлам
- Отправки нескольких файлов в одном сообщении

## 2. ИСПРАВЛЕНИЕ В COMPOSER.DART

### Добавить функциональность:
- Отображение превью выбранных файлов
- Кнопка "X" для удаления файла из списка
- Возможность добавить текст к файлам
- Кнопка отправки только когда есть файлы или текст

## 3. ЛОГИКА МУЛЬТИ-ФАЙЛОВ

### Сценарии использования:
1. **Одиночный файл** → отправляется сразу (как сейчас)
2. **Несколько файлов** → добавляются в список, отправляются вместе
3. **Файлы + текст** → отправляются в одном сообщении
4. **Отмена** → файл удаляется из списка

## 4. ПЛАН ИСПРАВЛЕНИЙ

### Шаг 1: Исправить хардкод team_id
### Шаг 2: Добавить логику мульти-файлов в chat_tab.dart
### Шаг 3: Обновить composer.dart для поддержки превью
### Шаг 4: Протестировать отправку и отображение

## 5. КЛЮЧЕВЫЕ ИЗМЕНЕНИЯ

### В chat_tab.dart:
```dart
// Добавить переменную для мульти-файлов
final List<LocalAttach> _pendingFiles = [];

// Логика отправки
if (_pendingFiles.isNotEmpty) {
  // Отправляем мульти-файлы
  await _sendMessageWithFilesRPC();
} else {
  // Обычная отправка текста
  await _sendMessage();
}
```

### В composer.dart:
```dart
// Показывать превью файлов
if (widget.attachedFiles.isNotEmpty) {
  ListView.builder(
    itemBuilder: (context, index) {
      return FilePreviewCard(
        file: widget.attachedFiles[index],
        onRemove: () => widget.onRemoveFile(index),
      );
    },
  );
}
```

## 6. ТЕСТИРОВАНИЕ

### Что проверить:
1. ✅ Одиночные файлы отправляются как раньше
2. ✅ Несколько файлов отправляются в одном сообщении
3. ✅ Файлы + текст отправляются вместе
4. ✅ Крестик удаляет файл из списка
5. ✅ MultiFileBubble отображает файлы правильно







